Adivinhe o Número: Escreva um programa em Java que gera um número aleatório
entre 1 e 100 (inclusive) e solicita ao usuário que tente adivinhar o número. Utilize
uma estrutura de repetição while para permitir várias tentativas até que o usuário
acerte o número. Forneça dicas ao usuário se o número digitado é maior ou menor
que o número aleatório.

import java.util.Scanner;
import java.util.Random;

public class POO_I {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        Random random = new Random();

        int numeroSecreto = random.nextInt(100) + 1; // gera número de 1 a 100
        int tentativa = 0;

        System.out.println("Tente adivinhar o numero (entre 1 e 100)!");

        while (tentativa != numeroSecreto) {
            System.out.print("Digite seu palpite: ");
            tentativa = scanner.nextInt();

            if (tentativa > numeroSecreto) {
                System.out.println("O numero secreto e menor!");
            } else if (tentativa < numeroSecreto) {
                System.out.println("O numero secreto e maior!");
            } else {
                System.out.println("Parabens! Você acertou o numero: " + numeroSecreto);
            }
        }
        scanner.close();
    }
}
